"""Agent package: Director / Interviewer / Grader / Reporter (PLANNING.md
Phase 9.1). Each agent owns one responsibility and degrades to a
deterministic fallback when the LLM is unavailable or offline (mock mode).
"""
